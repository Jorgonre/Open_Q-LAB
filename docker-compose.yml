services:
  qiskit:
    image: quantum-base:latest
    volumes:
      - .:/app
    working_dir: /app/qiskit
    command: python /app/benchmark.py

  cirq:
    image: quantum-base:latest
    volumes:
      - .:/app
    working_dir: /app/cirq
    command: python /app/benchmark.py

  pennylane:
    image: quantum-base:latest
    volumes:
      - .:/app
    working_dir: /app/pennylane
    command: python /app/benchmark.py

  myqlm:
    image: quantum-base:latest
    volumes:
      - .:/app
    working_dir: /app/myqlm
    command: python /app/benchmark.py

  tket:
    image: quantum-base:latest
    volumes:
      - .:/app
    working_dir: /app/tket
    command: python /app/benchmark.py