services:
  qiskit:
    build: ./qiskit
    volumes:
      - ./results:/results
  cirq:
    build: ./cirq
    volumes:
      - ./results:/results
  pennylane:
    build: ./pennylane
    volumes:
      - ./results:/results
  myqlm:
    build: ./myqlm
    volumes:
      - ./results:/results